services:
  api:
    build: .
    ports:
      - "8000:8000"
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
  db:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: hunter2
